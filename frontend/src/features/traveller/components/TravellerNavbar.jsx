import React from 'react';
import AppHeader from './AppHeader';
import SearchBar from './SearchBar';

export default function TravellerNavbar() {
  return (
    <AppHeader>
      <SearchBar />
    </AppHeader>
  );
}